external function puts ( ) { }

function write_chars_main ( ) {
    local i: long = '0'
    local j: long = '\b111'
    local k: long = '\x33'

    // can't yet do puts("Hello world!") because my code bad
    local str: nullstr = "Hello world!" // have to do this instead ):
    puts(str)
    
    return 0
}
